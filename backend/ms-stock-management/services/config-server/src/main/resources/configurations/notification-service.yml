server:
  port: 8040
spring:
  data:
    mongodb:
      username: ${MONGO_INITDB_ROOT_USERNAME:franck}
      password: ${MONGO_INITDB_ROOT_PASSWORD:franck}
      host: localhost
      port: 27017
      database: notification
      authentication-database: admin
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: paymentGroup,orderGroup
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: '*'
        spring.json.type.mapping: orderConfirmation:com.franck.ecommerce.kafka.order.OrderConfirmation,paymentConfirmation:com.franck.ecommerce.kafka.payment.PaymentConfirmation
  mail:
    host: localhost
    port: 1025
    username: ${SMTP_USERNAME:franck}
    password: ${SMTP_PASSWORD:franck}
    properties:
      mail:
        smtp:
          trust: "*"
        auth: true
        starttls:
          enabled: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000
